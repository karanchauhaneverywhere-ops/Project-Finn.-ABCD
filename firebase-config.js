// Fill this in with your own Firebase project's web app config to enable
// free cloud accounts (sign in + cross-device sync). Until you do, the site
// works exactly as before: fully free, local-only, no login required.
//
// Where to get these values: Firebase console → your project → Project
// settings → General → "Your apps" → Web app → SDK setup and configuration.
// These values are meant to be public in client-side code — Firebase
// protects your data with Firestore Security Rules (see firestore.rules),
// not by hiding this config. Full walkthrough in README.md.

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// Flip to true once you've replaced the placeholder values above.
export const firebaseConfigured = false;
